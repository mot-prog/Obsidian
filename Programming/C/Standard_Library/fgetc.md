```c
fgetc(FILE *stream)
```
Lit  le caractère suivant depuis le flux `stream` et le renvoie sous forme d'un `unsigned char` transformé en `int`, ou `EOF` en cas d'erreur ou de fin de fichier. Renvoie `NULL` en cas d'erreur.

