---
tags: [workflow, tools]
---
```bash
git submodule add ssh/https/link SubFolderName
```
add a `SubFolder` in a git repository. If the `SubFolder` has already a repository on the git, it just creates a link to it, so it doesn't to reload all the data.